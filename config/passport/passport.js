var bCrypt = require("bcrypt-nodejs");

module.exports = function (pasport, user){
    var User = user;
    var LocalStrategy = require('passport-local').Strategy;
}

passport.use("local-signup", new LocalStrategy(
    {
        username:Fi
    }
));